#include <stdio.h>

/**
 * main - Entry point
 * Description :Program  that prints all single digit numbers of base 10
 * Return: Always 0 (Success)
 */
int main(void)
{
int a;
for (a = 48; a <= 57; a++)
{
putchar(a);
}
putchar('\n');
return (0);
}
